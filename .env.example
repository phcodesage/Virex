# Copy this file to `.env` and paste your DeepSeek API key.
# On first launch Virex moves this key into the macOS Keychain, so it does not
# have to live in plaintext long-term. You can also set the key in Settings.
DEEPSEEK_API_KEY=sk-your-deepseek-key-here
